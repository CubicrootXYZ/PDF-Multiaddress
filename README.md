# PDF-Multiaddress
This tool takes a PDF DIN-Letter and inserts multiple addresses, so you never need to address all your letters by hand.

## Installation 
Use the provided exectuable or MSI-Installer for Windows systems. 

You also can execute the gui-test.py with Python 3, make sure to have all needed libraries installed (PyPDF2, reportlab)
